<div class="card mx-auto w-100">
    <div class="card-body">
        <h3 class="card-title">
            {{!-- we use the same way of post details, only difference is the url goes to edit--}}
            {{!-- clicck the title of the post goes to edit --}}
            <a href="/dashboard/edit/{{id}}" class="text-decoration-none">{{title}}</a>
        </h3>
        <p class="card-body fs-5">{{text}}</p>
        <p class="card-text text-end fs-6">
            <span id="span_username">{{user.name}}</span> created on {{format_date created_at}} !
        </p>

    </div>
</div>